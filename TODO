TODO
====

* implementation:
  * file path encoder
  * file descriptor decoder
  * move codec API to using off_t/off64_t instead of size_t since file codec
    rely on this to address files > 4GB
* unit tests:
  * lvstr array
  * map
  * map (decoder) and array discarding
  * test skip decoding
  * DPACK_ARRAY_LVSTR_SIZE(), DPACK_ARRAY_LVSTR_SIZE_MIN(), DPACK_ARRAY_LVSTR_SIZE_MAX()
* doc:
  * rationalize error code doc
  * lvstr and lvstr array
  * map
  * probe / peek tag
* gcc attributes
* utf8 support ?
